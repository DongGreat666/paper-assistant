# === Framework ===
reflex>=0.5.0

# === PDF Processing ===
marker-pdf>=1.6.0          # ML-based PDF → Markdown

# === Document Conversion ===
python-docx>=1.1.0         # Word (.docx) → Markdown

# === AI / LLM ===
httpx>=0.27.0              # Async HTTP client for direct API calls

# === Config / Env ===
python-dotenv>=1.0.0       # .env file loading

# === Utilities ===
python-dateutil>=2.9.0    # Date parsing
markdown>=3.5             # Markdown → HTML (bilingual view rendering)
playwright>=1.40          # Chromium-based Markdown PDF export
